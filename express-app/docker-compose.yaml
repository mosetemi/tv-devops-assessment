services:
  app:
    image: express-ts-app
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - 3000:3000
    restart: unless-stopped
